# 구글 로그인 설정 가이드

이메일 로그인/회원가입은 **지금 바로 작동**합니다. 구글 로그인만 아래 설정이 필요해요. 15분 정도 걸립니다.

내 정보 (미리 적어두면 편해요)
- 사이트 주소: `https://art-lyang.github.io/REPTILE/`
- Supabase 프로젝트 주소: `https://icjuhsktqcfloiqdfxtm.supabase.co`
- **콜백 주소(중요)**: `https://icjuhsktqcfloiqdfxtm.supabase.co/auth/v1/callback`

---

## 1단계 · Supabase 기본 설정 (먼저)

1. Supabase 대시보드 → 왼쪽 **Authentication** → **URL Configuration**
2. **Site URL** 에 입력: `https://art-lyang.github.io/REPTILE/`
3. **Redirect URLs** 에 아래 두 개를 각각 추가 (Add URL)
   - `https://art-lyang.github.io/REPTILE/**`
   - `http://localhost:8000/**` (로컬 테스트용, 선택)
4. **Save**

> 이 설정을 빼먹으면 로그인 후 엉뚱한 곳으로 튕기거나 "redirect not allowed" 오류가 납니다.

---

## 2단계 · 구글 클라우드 콘솔에서 키 발급

### 2-1. 프로젝트 만들기
1. https://console.cloud.google.com 접속 (구글 계정 로그인)
2. 상단 왼쪽 **프로젝트 선택** 드롭다운 → **새 프로젝트**
3. 이름은 아무거나 (예: `reptile-morph`) → **만들기**
4. 만들어진 프로젝트가 선택되어 있는지 상단에서 확인

### 2-2. OAuth 동의 화면 설정
1. 왼쪽 메뉴 **API 및 서비스** → **OAuth 동의 화면**
2. User Type: **외부(External)** 선택 → **만들기**
   - (조직 계정이 아니면 외부만 선택 가능해요)
3. 앱 정보 입력
   - **앱 이름**: 레오파드 모프 계산기 (사용자에게 보이는 이름)
   - **사용자 지원 이메일**: kmc612000@gmail.com
   - **개발자 연락처 정보**: kmc612000@gmail.com
4. **저장 후 계속** → 범위(Scopes) 화면에서 아무것도 추가하지 말고 **저장 후 계속**
5. 테스트 사용자 화면에서도 **저장 후 계속** → **대시보드로 돌아가기**

> **게시 상태**가 "테스트"로 되어 있으면 등록한 테스트 사용자만 로그인됩니다.
> 일반 사용자에게 열려면 동의 화면에서 **앱 게시(PUBLISH APP)** 를 눌러 "프로덕션"으로 바꾸세요.
> 이메일·프로필만 사용하므로 구글 심사 없이 바로 게시됩니다.

### 2-3. 사용자 인증 정보(클라이언트 ID) 만들기
1. 왼쪽 메뉴 **API 및 서비스** → **사용자 인증 정보**
2. 상단 **+ 사용자 인증 정보 만들기** → **OAuth 클라이언트 ID**
3. 애플리케이션 유형: **웹 애플리케이션**
4. 이름: 아무거나 (예: `reptile-web`)
5. **승인된 자바스크립트 원본** → URI 추가
   ```
   https://art-lyang.github.io
   ```
6. **승인된 리디렉션 URI** → URI 추가 (⚠️ 가장 중요, 오타 주의)
   ```
   https://icjuhsktqcfloiqdfxtm.supabase.co/auth/v1/callback
   ```
7. **만들기** → 팝업에 뜨는 **클라이언트 ID**와 **클라이언트 보안 비밀번호**를 복사
   (나중에 사용자 인증 정보 목록에서 다시 볼 수 있어요)

---

## 3단계 · Supabase에 구글 키 넣기

1. Supabase → **Authentication** → **Providers** → 목록에서 **Google** 클릭
2. **Enable Sign in with Google** 켜기
3. **Client ID** 와 **Client Secret** 에 2-3에서 복사한 값 붙여넣기
4. **Save**

---

## 4단계 · 테스트

1. `https://art-lyang.github.io/REPTILE/login.html` 접속
2. **Google로 로그인** 클릭 → 구글 계정 선택 → 다시 사이트로 돌아오면 성공
3. 계정 화면에 이메일이 보이면 완료

---

## 자주 나는 오류와 해결

**redirect_uri_mismatch**
구글에 등록한 리디렉션 URI가 틀렸습니다. `https://icjuhsktqcfloiqdfxtm.supabase.co/auth/v1/callback` 를 정확히(끝에 슬래시 없이) 넣었는지 확인하세요. 수정 후 반영까지 몇 분 걸릴 수 있어요.

**앱이 확인되지 않았습니다 (This app isn't verified)**
동의 화면이 "테스트" 상태입니다. OAuth 동의 화면에서 **앱 게시**를 누르거나, 테스트 사용자에 본인 계정을 추가하세요.

**로그인 후 이상한 페이지로 이동**
1단계의 Site URL / Redirect URLs 설정을 확인하세요.

**구글 로그인이 아직 설정되지 않았어요 (앱 메시지)**
Supabase Providers에서 Google이 꺼져 있거나 키가 저장되지 않은 상태입니다.

---

## 이메일 로그인 관련 참고

- Supabase 기본 메일 발송은 **시간당 몇 통** 제한이 있고 스팸함으로 잘 들어갑니다.
- 실제 서비스로 키우실 거면 **Authentication → Emails → SMTP Settings** 에서
  개인 메일 서버(예: Resend, SendGrid, 네이버웍스 등)를 연결하는 걸 권합니다.
- 빠른 테스트만 할 거면 **Authentication → Providers → Email** 에서
  "Confirm email"을 잠시 꺼두면 메일 인증 없이 바로 가입·로그인됩니다.
  (운영 시에는 다시 켜세요.)

---

## 파일 업로드 체크

저장소 루트에 아래 파일들이 **모두** 있어야 합니다.
```
index.html        계산기 + 관리자(#admin)
gecko-core.js     ★ 없으면 모프가 안 보입니다
breeding.html     브리딩 관리 (프리미엄)
login.html        로그인 / 회원가입   ← 이번에 추가
```
